describe('counter', () => {
  test('should divide 10 on 5', () => {
    const result = 10 / 5;
    expect(result).toBe(2);
  });

  test('should divide 9 on 3', () => {
    const result = 9 / 3;
    expect(result).toBe(3);
  });
});
